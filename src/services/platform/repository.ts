/**
 * The GitHub repository this build belongs to, in `owner/name` form.
 *
 * Update checks and the "report this error" link both address it, so it lives
 * in one place: a stale copy makes update checks fail every time and points
 * bug reports at a repository that does not exist.
 */
export const GITHUB_REPOSITORY = "ImmortalAI/easy-digital-book";

export const GITHUB_REPOSITORY_URL = `https://github.com/${GITHUB_REPOSITORY}`;
